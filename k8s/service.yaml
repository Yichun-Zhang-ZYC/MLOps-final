apiVersion: v1
kind: Service
metadata:
  name: credit-default-api
  namespace: mlops-final
spec:
  selector:
    app: credit-default-api
  ports:
    - port: 80
      targetPort: 8000
  type: LoadBalancer
